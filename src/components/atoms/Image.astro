---
import type {LocalImageProps, RemoteImageProps} from 'astro:assets';
import {Image as AstroImage} from 'astro:assets';

interface Props extends (typeof AstroImage) {}
---

<AstroImage {...Astro.props}/>
